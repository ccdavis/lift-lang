// Test: Arithmetic on Flt fields from struct
{
    type Point = struct (x: Flt, y: Flt);

    let p = Point(x: 3.0, y: 4.0);

    // Test arithmetic directly
    let result: Flt = p.x * p.x + p.y * p.y;

    output(result);  // Should output 25.0

    0
}
